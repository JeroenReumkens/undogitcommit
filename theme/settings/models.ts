export interface Colors {
  primary: string;
}

export interface Fonts {
  primary: string;
}

export interface Grid {
  maxWidth: string;
  gutter: string;
  maxWidthAndGutter: string;
}

export interface Spacing {
  base: string;
}

export interface Transitions {
  base: string;
}
